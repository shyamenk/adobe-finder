import FileUpload from '@/components/utils/FileUpload';
import { FC } from 'react';

const page: FC = () => {
  return (
    <div className="h-screen">
      <FileUpload />
    </div>
  );
};

export default page;
