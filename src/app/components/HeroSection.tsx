import { FC } from 'react';

const HeroSection: FC = () => {
  return (
    <section className="bg-mode-light text-mode-dark dark:bg-mode-dark dark:text-mode-light"></section>
  );
};

export default HeroSection;
